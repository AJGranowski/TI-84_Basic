// GREATEST COMMON DENOMINATOR
// A program for taking test with (since the gcd function only takes two arguments)
// Takes inputs via L₁

If not(dim(L₁
	Return

L₁(1
If 2>dim(L₁
	Goto A

gcd(L₁(1),L₁(2→X
If 2=dim(L₁
	Goto A

For(I,3,dim(L₁))
	gcd(X,L₁(I→X
End

X
Lbl A
DelVar IAns